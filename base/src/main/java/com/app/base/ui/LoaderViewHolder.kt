package com.app.base.ui

/*class LoaderViewHolder(private val binding: LoaderListItemBinding, private val callback: LoaderViewItemCallback) :
    ViewHolder(binding.root) {
    val vm = LoaderItemViewModel()

    init {
        this.binding.callback = callback
        this.binding.vm = vm
        binding.executePendingBindings()
    }

    fun bind(networkState: PageState) {
        vm.setData(networkState)
        binding.executePendingBindings()
    }
}*/
